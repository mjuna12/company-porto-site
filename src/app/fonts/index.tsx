import local from 'next/font/local';

export const formular = local({
  src: [
    { path: './Formular.ttf'},
  ],
  
  display: 'swap'
});

export const formularBlack = local({
  src: [
    { path: './Formular-Black.ttf'}
  ],
  
  display: 'swap'
});